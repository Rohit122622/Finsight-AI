import { defineConfig, devices } from "@playwright/test";

/**
 * FinSentry AI — Phase 5G Playwright E2E Configuration
 * Tests the complete user flow: Login → Upload → Processing → Document → Extraction →
 * Red Flag/Research → Comparison → Research → Report → Dashboard/session details → Download
 */
export default defineConfig({
  testDir: "./e2e",
  fullyParallel: false,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 1 : 0,
  workers: 1,
  reporter: [["list"], ["html", { open: "never" }]],
  timeout: 120000,
  use: {
    baseURL: process.env.E2E_BASE_URL || "http://localhost:5173",
    trace: "on-first-retry",
    screenshot: "only-on-failure",
  },
  projects: [
    {
      name: "chromium",
      use: { ...devices["Desktop Chrome"] },
    },
  ],
});
